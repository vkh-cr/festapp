# Wave 0 decision gate — 2026-08-27

Status: **BLOCKED — do not provision or mutate production**

The approved read-only Management API inventory completed for both pinned
sources. Private `0600` evidence is stored outside Git in
`/tmp/festapp-wave0.ItFQux`; it contains catalog metadata and aggregate counts,
not row payloads or production identities. Move or encrypt it before host cleanup
if it must be retained.

## Sanitized results

| Metric | `default` | `a` |
|---|---:|---:|
| Project ref | `kjdpmixlnhntmxjedpxh` | `lwfpdjxsdmkfyrzqbrlk` |
| PostgreSQL | 17.6 | 15.1 |
| Database bytes | 482,634,899 | 2,359,698,223 |
| Catalog relations | 120 | 150 |
| Counted rows | 889,647 | 3,708,238 |
| Edge Functions | 15 | 23 |
| Active / configured connections at snapshot | 15 / 60 | 15 / 60 |
| Schema SHA-256 | `02d7e56f9fce9cc7863b4160cb8944991384784aa9291a7f191a393354e00ee1` | `72b06ccabee8d19748901c112021b3b99545618bf1d78ad35fc1823b22a6ca53` |

The first deterministic comparison reports 1,579 unresolved catalog
differences: 120 relations, 450 columns, 149 constraints, 825 functions, 27
policies and 8 extensions. Some function/extension differences may be explained
by the PostgreSQL major and provider runtime, but none is considered resolved
until classified against the canonical repository schema.

Known checked-out production mappings are:

| Branch | Source | Organization | Occasion | Image project |
|---|---|---:|---|---|
| `prod/csmostrava2026` | `a` | 9 | `csmostrava2026` | `a` |
| `prod/hvezdamorska` | `a` | 4 | unset | `a` |

`main` points to `default`, organization 1, with no forced occasion. This is not
yet a complete inventory of all production/release refs and therefore does not
satisfy the all-tenant write-authority requirement.

## Open blockers

- PostgreSQL majors differ (17 versus 15). The target runtime and restore/import
  boundary require an explicit compatibility decision; the cutover may not
  silently upgrade either restored source.
- Schema drift is not classified or explained.
- Provider history for Storage size/growth, WAL rate and peak connections is
  missing; the combined 2.84 GB database snapshot alone is insufficient sizing
  evidence.
- Auth/provider/MFA, UUID/natural-key and Storage object collision passes have
  not been approved or completed.
- The production branch/config and write-authority matrices are incomplete.
- RPO, RTO, cloud retention, DNS/Cloudflare owner, secret owner and on-call owner
  are not recorded.
- The journaled-hybrid versus full-maintenance-freeze decision cannot be made
  until every `a` writer and enforceable client adoption boundary is known.

The next permitted work is read-only evidence collection and drift/writer
classification. Creating a Hetzner server, changing DNS, deploying SQL, removing
grants or freezing either cloud is outside this gate.
