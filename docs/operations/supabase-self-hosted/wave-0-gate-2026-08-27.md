# Wave 0 decision gate — 2026-08-27

Status: **WAVE 0 BLOCKED ON ALL OPEN GATES — do not provision, mutate or delete**

The approved read-only Management API inventory completed for both pinned
sources. Private `0600` evidence is stored outside Git in
`/tmp/festapp-wave0-approved.hsng4B`; it contains catalog metadata, aggregate
counts and HMAC-pseudonymized collision evidence,
not row payloads or production identities. Move or encrypt it before host cleanup
if it must be retained.

## Sanitized results

| Metric | `default` | `a` |
|---|---:|---:|
| Project ref | `kjdpmixlnhntmxjedpxh` | `lwfpdjxsdmkfyrzqbrlk` |
| PostgreSQL | 17.6 | 15.1 |
| Database bytes | 482,651,283 | 2,359,976,751 |
| Current WAL bytes | 486,539,637 | 218,103,808 |
| Storage objects / bytes | 264 / 85,091,285 | 935 / 436,768,281 |
| Auth users | 231 | 6,980 |
| Catalog relations | 120 | 150 |
| Counted rows | 889,685 | 3,708,495 |
| Edge Functions | 15 | 23 |
| Active / configured connections at snapshot | 15 / 60 | 15 / 60 |
| Schema SHA-256 | `c232048f13ca567800d896182fd49ea69bdaf357ad3eba0f75ecf4d539e4379c` | `d660dd0ff521640a34034ea57acf368241a556806fbd171f0f4366f5c68a5f3e` |

The corrected deterministic comparison reports 2,231 unresolved catalog
differences: 30 relations, 450 columns, 149 constraints, 825 functions, 27
policies, 738 grants, 8 extensions and 4 roles. Publications and collations have
no structural differences. All remaining differences require classification
against the canonical repository schema.

The private Auth/Storage pass found:

- 13 verified-email matches with different source UUIDs; each received an
  explicit identity merge decision after per-user Auth-state validation;
- zero equal UUIDs with different e-mails;
- zero cross-source provider identity-key collisions;
- zero cross-source verified-phone collisions;
- zero equal Storage bucket/key pairs across the sources;
- object-content SHA-256 validation remains required during export/copy.

The collision evidence checksum is
`f965c5faf6bd1dde1ed16da84d5e3fcd3c0eacf7e49fd857d0064bf767fe97f0`
(collision report contract v2; older reports are rejected).
It proves that all 13 pairs are e-mail-provider only, have no MFA, phone, SSO,
anonymous or pending confirmation/recovery/change/reauthentication state. The
13 Auth collisions are covered by cryptographically bound private decision
manifest `df6793daa2f9d7a715668d7adfc3e40688d0d4897f53852ad97e19a8b9932275`:
all preserve the `default` UUID/password hash, remap source `a`, merge
memberships and require reset of the discarded source password. Decision
validation is 13 resolved / 0 unresolved; no production identity was changed.

Storage authority is resolved for this migration: all remaining cloud Supabase
Storage objects move additively into canonical self-hosted Supabase Storage;
existing R2 image buckets and the independent P3/P4 cutover remain unchanged.

The researched latest-stable candidate as of 2026-08-27 is Supabase `self-hosted/v0.8.0` at commit
`241bb11c0627f2981746d37033f57dbfa81d29b0`, pinned by image digest. It uses
PostgreSQL 17 and therefore matches canonical `default`; PostgreSQL 15 source
`a` remains a logical staging import. PostgreSQL 18 is deferred to a separate
post-cutover release because no selected stable Supabase bundle tests it.
The online pin verifier must pass again immediately before provisioning and each
rehearsal; this report is not permission to deploy a stale candidate later.

Known checked-out production mappings are:

| Branch | Source | Organization | Occasion | Image project |
|---|---|---:|---|---|
| `prod/csmostrava2026` | `a` | 9 | `csmostrava2026` | `a` |
| `prod/hvezdamorska` | `a` | 4 | unset | `a` |

`main` points to `default`, organization 1, with no forced occasion. This is not
yet a complete inventory of all production/release refs and therefore does not
satisfy the all-tenant write-authority requirement.

## Open blockers

- PostgreSQL majors differ (17 versus 15). The approved boundary is PG17 target,
  native `default` restore and logical staging import of `a`; rehearsal must
  still prove that boundary and extension compatibility before provisioning.
- Schema drift is not classified or explained.
- Provider history for growth, WAL rate and peak connections is missing. Current
  DB + WAL + Storage totals are known, but historical capacity evidence is not.
- UUID/natural-key collision coverage outside Auth and Storage object SHA-256
  verification remain open. Auth identity decisions are resolved in private
  evidence but are not yet exercised by a rehearsal.
- The production branch/config and write-authority matrices are incomplete.
- Repository discovery currently has 68 unreviewed write candidates, including
  12 direct-DML files. Nine production Flutter data paths must move behind
  explicit journaled RPCs before a hybrid can be safe.
- RPO, RTO, cloud retention, DNS/Cloudflare owner, secret owner and on-call owner
  are not recorded.
- The journaled-hybrid versus full-maintenance-freeze decision cannot be made
  until every `a` writer and enforceable client adoption boundary is known.

The next permitted work is read-only evidence collection and drift/writer
classification. Creating a Hetzner server, changing DNS, deploying SQL, removing
grants or freezing either cloud is outside this gate.
