
# Deployment Configuration
DOMAIN=vstupenky.online

# DEPLOY_TARGET picks the CI deploy job in .github/workflows/deploy.yml:
#   cloudflare | skip
# Leave blank or set to "skip" to opt main / non-prod branches out of CI deploys.
DEPLOY_TARGET=

# Cloudflare Pages project name (required when DEPLOY_TARGET=cloudflare).
CLOUDFLARE_PROJECT_NAME=


# Application Configuration
APP_NAME="Festapp"
APP_TITLE_SHORT="Festapp"
APP_DESCRIPTION="Platforma pro správu akcí, programu a vstupenek."
IOS_BUNDLE_NAME="Festapp"
VERSION=0.19.93+453
FLUTTER_VERSION=3.47.2

# Public tenant identities. These values are propagated into native and web
# build inputs by apply_config.sh; generated files are never their owner.
ANDROID_APPLICATION_ID=fstapp.fstapp
IOS_BUNDLE_ID=festapp.festapp
IOS_DEVELOPMENT_TEAM=8WKBB6L8LT
IOS_PROVISIONING_PROFILE=
IOS_ONESIGNAL_PROVISIONING_PROFILE=
IOS_ONESIGNAL_APP_GROUP=group.festapp.festapp.onesignal
IOS_ASSOCIATED_DOMAIN=vstupenky.online
ONESIGNAL_NATIVE_APP_ID=
ONESIGNAL_WEB_APP_ID=73f77f22-961a-4ded-9647-e33a7ac14f90
ONESIGNAL_SAFARI_WEB_ID=web.onesignal.auto.34cabfa2-ddd9-46d0-b8b2-6fad793020e0
PUSH_APP_GENERATION=

# Brand assets remain branch-owned; shared widgets consume these configured
# canonical paths instead of hard-coding a tenant's filenames.
LOGO_ASSET=assets/icons/fstapplogo.svg
DARK_LOGO_ASSET=assets/icons/fstapplogo.dark.svg
PROGRAM_LOGO_ASSET=assets/icons/fstapplogo.svg
WEB_LOADING_LOGO_ASSET=android-chrome-192x192.png

SUPABASE_URL=https://kjdpmixlnhntmxjedpxh.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImtqZHBtaXhsbmhudG14amVkcHhoIiwicm9sZSI6ImFub24iLCJpYXQiOjE3MDE5NDI5NzEsImV4cCI6MjAxNzUxODk3MX0.06nTXCL-i1GxLckfEyCNlVVwt62QTzKUezqmsYSR_MI
# Stable across a backend-hostname cutover so imported browser refresh tokens
# can be exchanged by the new Auth service before the legacy cloud is retired.
SUPABASE_AUTH_STORAGE_KEY=sb-kjdpmixlnhntmxjedpxh-auth-token
# Optional one-way rollout contract. When populated, the release starts on
# SUPABASE_URL and accepts only the exact canonical activation document emitted
# at WEB_LINK/backend-activation.json. Set phase=canonical at the operational
# cutover; installed transition clients do not require another store release.
BACKEND_ACTIVATION_TENANT_ID=
BACKEND_ACTIVATION_PHASE=
BACKEND_ACTIVATION_CANONICAL_SUPABASE_URL=
BACKEND_ACTIVATION_CANONICAL_SUPABASE_ANON_KEY=
BACKEND_ACTIVATION_CANONICAL_ORGANIZATION_ID=
# Generated into both clients; never set manually. It binds the complete
# canonical profile and lets the completed-bundle verifier reject stale output.
BACKEND_ACTIVATION_CANONICAL_PROFILE_SHA256=
ORGANIZATION_ID=1
FLUTTER_APP_URL=
IS_APP_SUPPORTED=true
WEB_IS_ALL_UNIT=false
WEB_SUPPORTED_LANGUAGES=cs
WEB_LINK=https://vstupenky.online

# Required public legal/support contract. apply_config, web build, and the
# production deployment probe fail closed if any canonical page is missing,
# stale, branded for another tenant, or served by the SPA fallback.
PRIVACY_URL=https://vstupenky.online/privacy/
PRIVACY_CHOICES_URL=https://vstupenky.online/privacy/choices/
TERMS_URL=https://vstupenky.online/terms/
SUPPORT_URL=https://vstupenky.online/support/
DELETE_ACCOUNT_URL=https://vstupenky.online/delete-account/
FORCE_OCCASION_LINK=
SYNC_HEAD_ORIGIN=
SYNC_ASSET_ORIGIN=
IMAGE_API_URL=https://image-api.festapp.net
IMAGE_PROJECT_ID=default
PWA_CORE_CACHE_BUDGET_BYTES=26214400
PWA_KNOWN_CACHE_BUDGET_BYTES=62914560

# Theme Configuration
THEME_SEED_1=#253759
THEME_SEED_2=#80BDF2
THEME_SEED_3=#4465A6
THEME_SEED_4=#0D0D0D

# Font Configuration
FONT_FAMILY_BASE=Futura
FORM_FONT_SCALE=1.2
